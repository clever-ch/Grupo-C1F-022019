import { CustomerWalletDTO } from './customer-wallet-dto';

describe('CustomerWalletDTO', () => {
  it('should create an instance', () => {
    expect(new CustomerWalletDTO()).toBeTruthy();
  });
});
